/* Round 179 - Production Appointment Excel parser (XLSB/XLSX). */
importScripts("https://cdn.sheetjs.com/xlsx-0.20.3/package/dist/xlsx.full.min.js");

const DAY_MS=86400000;
function cleanText(value){return String(value??"").replace(/\u00a0/g," ").replace(/\s+/g," ").trim()}
function normalizeHeader(value){return cleanText(value).toUpperCase()}
function uniqueStrings(value){const arr=Array.isArray(value)?value:String(value??"").split(/[\n,;|]+/),out=[],seen=new Set();for(const item of arr){const s=cleanText(item);if(!s)continue;const k=s.toUpperCase();if(seen.has(k))continue;seen.add(k);out.push(s)}return out}
function pad2(n){return String(n).padStart(2,"0")}
function isLeap(y){return y%4===0&&(y%100!==0||y%400===0)}
function daysInMonth(y,m){return [31,isLeap(y)?29:28,31,30,31,30,31,31,30,31,30,31][m-1]||0}
function validYmd(y,m,d){return Number.isInteger(y)&&Number.isInteger(m)&&Number.isInteger(d)&&y>=1900&&y<=2200&&m>=1&&m<=12&&d>=1&&d<=daysInMonth(y,m)}
function ymd(y,m,d){return `${String(y).padStart(4,"0")}-${pad2(m)}-${pad2(d)}`}
function excelSerialToDate(serial,date1904){const n=Number(serial);if(!Number.isFinite(n))return null;const whole=Math.floor(n);if(date1904){const dt=new Date(Date.UTC(1904,0,1)+whole*DAY_MS);return ymd(dt.getUTCFullYear(),dt.getUTCMonth()+1,dt.getUTCDate())}if(whole===60)return null;const adjusted=whole>60?whole-1:whole,dt=new Date(Date.UTC(1899,11,31)+adjusted*DAY_MS);return ymd(dt.getUTCFullYear(),dt.getUTCMonth()+1,dt.getUTCDate())}
function parseTextDate(text,acceptedFormats){const s=cleanText(text),formats=new Set(acceptedFormats||["DD.MM.YYYY","DD/MM/YYYY","YYYY-MM-DD"]);let m;if(formats.has("DD.MM.YYYY")&&(m=s.match(/^(\d{1,2})\.(\d{1,2})\.(\d{4})$/))){const d=+m[1],mo=+m[2],y=+m[3];return validYmd(y,mo,d)?{ok:true,value:ymd(y,mo,d),mode:"TEXT_DD.MM.YYYY"}:{ok:false,reason:"วันที่ไม่ถูกต้อง"}}if(formats.has("DD/MM/YYYY")&&(m=s.match(/^(\d{1,2})\/(\d{1,2})\/(\d{4})$/))){const d=+m[1],mo=+m[2],y=+m[3];return validYmd(y,mo,d)?{ok:true,value:ymd(y,mo,d),mode:"TEXT_DD/MM/YYYY"}:{ok:false,reason:"วันที่ไม่ถูกต้อง"}}if(formats.has("YYYY-MM-DD")&&(m=s.match(/^(\d{4})-(\d{1,2})-(\d{1,2})$/))){const y=+m[1],mo=+m[2],d=+m[3];return validYmd(y,mo,d)?{ok:true,value:ymd(y,mo,d),mode:"TEXT_YYYY-MM-DD"}:{ok:false,reason:"วันที่ไม่ถูกต้อง"}}return{ok:false,reason:s?"รูปแบบวันที่ไม่รองรับ":"วันที่ว่าง"}}
function safeRaw(cell){const v=cell?.v;if(v instanceof Date)return v.toISOString();if(v===undefined||v===null)return"";if(typeof v==="number")return Number.isFinite(v)?String(v):"";return cleanText(v)}
function parseDateCell(cell,opts={}){const v=cell?.v;if(v instanceof Date&&!Number.isNaN(v.getTime())){const y=v.getUTCFullYear(),m=v.getUTCMonth()+1,d=v.getUTCDate();return validYmd(y,m,d)?{ok:true,value:ymd(y,m,d),mode:"DATE_OBJECT",raw:safeRaw(cell)}:{ok:false,reason:"วันที่ไม่ถูกต้อง",raw:safeRaw(cell)}}if(typeof v==="number"&&Number.isFinite(v)){if(Number.isInteger(v)&&v>=19000101&&v<=22001231){const s=String(v),y=+s.slice(0,4),m=+s.slice(4,6),d=+s.slice(6,8);if(validYmd(y,m,d))return{ok:true,value:ymd(y,m,d),mode:"NUMBER_YYYYMMDD",raw:safeRaw(cell)}}const ds=String(opts.dateSystem||"AUTO").toUpperCase(),date1904=ds==="1904"?true:ds==="1900"?false:!!opts.workbookDate1904,value=excelSerialToDate(v,date1904);return value?{ok:true,value,mode:date1904?"EXCEL_SERIAL_1904":"EXCEL_SERIAL_1900",raw:safeRaw(cell)}:{ok:false,reason:"วันที่ Excel ไม่ถูกต้อง",raw:safeRaw(cell)}}const parsed=parseTextDate(v,opts.acceptedTextFormats);return{...parsed,raw:safeRaw(cell)}}
function parseTimeText(text){const s=cleanText(text);if(!s)return{ok:false,reason:"เวลาว่าง"};const m=s.match(/^(\d{1,2})[:.](\d{2})(?::(\d{2}))?$/);if(!m)return{ok:false,reason:"รูปแบบเวลาไม่ถูกต้อง"};const h=+m[1],min=+m[2],sec=+(m[3]||0);if(h<0||h>23||min<0||min>59||sec<0||sec>59)return{ok:false,reason:"เวลาอยู่นอกช่วง 00:00-23:59"};return{ok:true,value:h*60+min,mode:"TEXT_TIME",seconds:sec}}
function looksLikeTimeFormat(z){return /(^|[^a-z])[hms]+([^a-z]|$)/i.test(String(z||""))}
function parseTimeCell(cell){const v=cell?.v;if(typeof v==="number"&&Number.isFinite(v)){if(v>=0&&v<1){let mins=Math.round(v*1440);if(mins===1440)mins=0;return{ok:true,value:mins,mode:"EXCEL_FRACTION",raw:safeRaw(cell)}}const frac=((v%1)+1)%1;if(frac>0&&looksLikeTimeFormat(cell?.z)){let mins=Math.round(frac*1440);if(mins===1440)mins=0;return{ok:true,value:mins,mode:"EXCEL_DATETIME_FRACTION",raw:safeRaw(cell)}}return{ok:false,reason:"ค่าตัวเลขเวลาไม่ถูกต้อง",raw:safeRaw(cell)}}const p=parseTimeText(v);return{...p,raw:safeRaw(cell)}}
function identityCell(cell){const v=cell?.v,w=cleanText(cell?.w);if(typeof v==="number"&&Number.isFinite(v)){if(Number.isInteger(v)){if(w&&/^\d+$/.test(w.replace(/,/g,""))&&!/[eE][+-]?\d/.test(w))return w.replace(/,/g,"");return String(Math.trunc(v))}return cleanText(w||String(v))}return cleanText(v)}
function normalizeConfig(input){const cfg=JSON.parse(JSON.stringify(input||{}));cfg.sheetName=cleanText(cfg.sheetName||"raw_data");cfg.sheetAliases=uniqueStrings(cfg.sheetAliases||[]);cfg.warehouse=cfg.warehouse||{};cfg.warehouse.matchMode=String(cfg.warehouse.matchMode||"STARTS_WITH").toUpperCase()==="EXACT"?"EXACT":"STARTS_WITH";cfg.warehouse.values=uniqueStrings(cfg.warehouse.values||["906"]).map(v=>v.toUpperCase());cfg.timeReference=String(cfg.timeReference||"PERIOD").toUpperCase()==="FROM"?"FROM":"PERIOD";cfg.date=cfg.date||{};cfg.date.dateSystem=String(cfg.date.dateSystem||"AUTO").toUpperCase();cfg.date.acceptedTextFormats=uniqueStrings(cfg.date.acceptedTextFormats||["DD.MM.YYYY","DD/MM/YYYY","YYYY-MM-DD"]);cfg.fields=cfg.fields||{};for(const key of["dc","date","period","from","to","po","appointment","vendor","carrier"]){cfg.fields[key]=cfg.fields[key]||{};cfg.fields[key].header=cleanText(cfg.fields[key].header);cfg.fields[key].aliases=uniqueStrings(cfg.fields[key].aliases||[])}return cfg}
function warehouseMatches(dc,cfg){const value=cleanText(dc).toUpperCase(),values=cfg?.warehouse?.values||[];if(!value||!values.length)return false;return cfg.warehouse.matchMode==="EXACT"?values.includes(value):values.some(prefix=>value.startsWith(prefix))}
function fieldCandidates(field){return uniqueStrings([field?.header,...(field?.aliases||[])])}
function buildHeaderMap(headerCells,cfg){const normalized=headerCells.map(c=>normalizeHeader(c?.v)),map={},missing=[];for(const[key,field]of Object.entries(cfg.fields)){const candidates=fieldCandidates(field).map(normalizeHeader).filter(Boolean);let idx=-1;for(const candidate of candidates){idx=normalized.indexOf(candidate);if(idx>=0)break}if(idx>=0)map[key]=idx;else missing.push(key)}return{map,missing,headers:normalized}}
function headerScore(headerCells,cfg){const{map}=buildHeaderMap(headerCells,cfg),keys=Object.keys(map),required=["dc","date","appointment"],requiredHits=required.filter(k=>k in map).length;return{score:keys.length,requiredHits,map}}
function checkConflict(g,key,value,rowNo){if(value===null||value===undefined)return;if(g[key]===null||g[key]===undefined){g[key]=value;return}if(g[key]!==value&&!g.warnings.some(w=>w.startsWith(key)))g.warnings.push(`${key.toUpperCase()} ไม่ตรงกันใน Appointment เดียวกัน (แถว ${rowNo})`)}
function inc(obj,key){obj[key]=(obj[key]||0)+1}
function aggregateRows(rows,cfg){const groups=new Map(),errors=[],stats={sourceRows:0,warehouseRows:0,validRows:0,invalidRows:0,dateModes:{},periodModes:{},fromModes:{},toModes:{}};const refKey=cfg.timeReference==="FROM"?"from":"period";for(const row of rows){stats.sourceRows++;const dc=identityCell(row.dc);if(!warehouseMatches(dc,cfg))continue;stats.warehouseRows++;const appointment=identityCell(row.appointment),po=identityCell(row.po),vendor=cleanText(row.vendor?.v),carrier=cleanText(row.carrier?.v),date=parseDateCell(row.date,{dateSystem:cfg.date.dateSystem,workbookDate1904:cfg.workbookDate1904,acceptedTextFormats:cfg.date.acceptedTextFormats}),period=parseTimeCell(row.period),from=parseTimeCell(row.from),to=parseTimeCell(row.to),ref=refKey==="from"?from:period,rowNo=row.__row||0,rowErrors=[];if(!dc)rowErrors.push("รหัสคลังว่าง");if(!appointment)rowErrors.push("Appointment ว่าง");if(!date.ok)rowErrors.push(`DATE: ${date.reason}`);if(!ref.ok)rowErrors.push(`${cfg.timeReference}: ${ref.reason}`);if(rowErrors.length){stats.invalidRows++;errors.push({row:rowNo,appointment,dc,message:rowErrors.join(" · "),rawDate:date.raw||""});continue}stats.validRows++;inc(stats.dateModes,date.mode);if(period.ok)inc(stats.periodModes,period.mode);if(from.ok)inc(stats.fromModes,from.mode);if(to.ok)inc(stats.toModes,to.mode);const key=`${dc}\u001f${date.value}\u001f${appointment}`;let g=groups.get(key);if(!g){g={key,dc,appointment,date:date.value,period:period.ok?period.value:null,from:from.ok?from.value:null,to:to.ok?to.value:null,referenceType:cfg.timeReference,referenceMinute:ref.value,pos:new Set(),vendors:new Set(),carriers:new Set(),lines:new Map(),sourceRows:0,warnings:[],raw:{date:date.raw,period:period.raw,from:from.raw,to:to.raw}};groups.set(key,g)}else{checkConflict(g,"period",period.ok?period.value:null,rowNo);checkConflict(g,"from",from.ok?from.value:null,rowNo);checkConflict(g,"to",to.ok?to.value:null,rowNo);if(g.referenceMinute!==ref.value&&!g.warnings.some(w=>w.includes("เวลาอ้างอิง")))g.warnings.push(`เวลาอ้างอิงไม่ตรงกันใน Appointment เดียวกัน (แถว ${rowNo})`)}g.sourceRows++;if(po)g.pos.add(po);if(vendor)g.vendors.add(vendor);if(carrier)g.carriers.add(carrier);const lineKey=[po,vendor,carrier].join("\u001f");if(po||vendor||carrier)g.lines.set(lineKey,{po,vendor,carrier})}const appointments=[...groups.values()].map(g=>({...g,pos:[...g.pos],vendors:[...g.vendors],carriers:[...g.carriers],lines:[...g.lines.values()]})).sort((a,b)=>a.date.localeCompare(b.date)||a.referenceMinute-b.referenceMinute||a.appointment.localeCompare(b.appointment,undefined,{numeric:true}));return{appointments,errors,stats}}
function resolveSheet(wb,cfg){const names=wb.SheetNames||[],wanted=[cfg.sheetName,...(cfg.sheetAliases||[])].filter(Boolean);for(const w of wanted){const n=names.find(x=>x.trim().toUpperCase()===String(w).trim().toUpperCase());if(n){const hr=findHeaderRow(wb.Sheets[n],cfg);if(hr>=0)return{name:n,headerRow:hr,method:n===cfg.sheetName?"PRIMARY_NAME":"ALIAS_NAME"}}}let best=null;for(const n of names){const ws=wb.Sheets[n],hr=findHeaderRow(ws,cfg);if(hr<0)continue;const score=scoreHeaderRow(ws,hr,cfg);if(!best||score>best.score)best={name:n,headerRow:hr,method:"HEADER_DETECTED",score}}return best&&best.score>=5?best:null}
function findHeaderRow(ws,cfg){const range=XLSX.utils.decode_range(ws?.["!ref"]||"A1:A1"),end=Math.min(range.e.r,range.s.r+19);let best={r:-1,score:-1,req:0};for(let r=range.s.r;r<=end;r++){const cells=[];for(let c=range.s.c;c<=range.e.c;c++)cells.push(ws[XLSX.utils.encode_cell({r,c})]||{});const s=headerScore(cells,cfg);if(s.requiredHits>=3&&s.score>best.score)best={r,score:s.score,req:s.requiredHits}}return best.r}
function scoreHeaderRow(ws,r,cfg){const range=XLSX.utils.decode_range(ws?.["!ref"]||"A1:A1"),cells=[];for(let c=range.s.c;c<=range.e.c;c++)cells.push(ws[XLSX.utils.encode_cell({r,c})]||{});return headerScore(cells,cfg).score}
async function hashBuffer(buffer){const digest=await crypto.subtle.digest("SHA-256",buffer.slice(0));return[...new Uint8Array(digest)].map(b=>b.toString(16).padStart(2,"0")).join("")}
function parserSelfTest(){try{return excelSerialToDate(1,false)==="1900-01-01"&&excelSerialToDate(59,false)==="1900-02-28"&&excelSerialToDate(61,false)==="1900-03-01"&&parseTextDate("18.08.2026",["DD.MM.YYYY"]).value==="2026-08-18"&&parseTimeCell({v:0.25}).value===360}catch{return false}}

self.onmessage=async event=>{const{type,buffer,fileName,config}=event.data||{};if(type!=="PARSE"||!buffer)return;try{if(!parserSelfTest())throw new Error("การตรวจวันที่และเวลาไม่ผ่าน");postMessage({type:"PROGRESS",message:"กำลังเตรียมไฟล์"});const fileHash=await hashBuffer(buffer);postMessage({type:"PROGRESS",message:"กำลังอ่านข้อมูล"});const wb=XLSX.read(buffer,{type:"array",cellDates:false,cellNF:true,cellText:true,dense:false}),cfg=normalizeConfig(config||{});cfg.workbookDate1904=!!wb?.Workbook?.WBProps?.date1904;const resolved=resolveSheet(wb,cfg);if(!resolved)throw new Error("ไม่พบชีทที่ตรงกับการตั้งค่า");const ws=wb.Sheets[resolved.name],headerRow=resolved.headerRow,range=XLSX.utils.decode_range(ws["!ref"]||"A1:A1"),headerCells=[];for(let c=range.s.c;c<=range.e.c;c++)headerCells.push(ws[XLSX.utils.encode_cell({r:headerRow,c})]||{});const header=buildHeaderMap(headerCells,cfg),critical=["dc","date","appointment",cfg.timeReference==="FROM"?"from":"period"],missing=critical.filter(k=>!(k in header.map));if(missing.length)throw new Error("ไม่พบคอลัมน์ที่จำเป็น: "+missing.map(k=>cfg.fields[k]?.header||k).join(", "));const rows=[],total=Math.max(0,range.e.r-headerRow);for(let r=headerRow+1;r<=range.e.r;r++){if((r-headerRow)%3000===0)postMessage({type:"PROGRESS",message:`กำลังตรวจข้อมูล ${Math.min(r-headerRow,total).toLocaleString()} / ${total.toLocaleString()} แถว`});const row={__row:r+1};for(const key of Object.keys(cfg.fields)){const pos=header.map[key];row[key]=Number.isInteger(pos)?(ws[XLSX.utils.encode_cell({r,c:range.s.c+pos})]||{}):{}}rows.push(row)}const result=aggregateRows(rows,cfg),dateValues=result.appointments.map(a=>a.date).sort();postMessage({type:"RESULT",result:{fileName,fileHash,sourceSheet:resolved.name,headerRow:headerRow+1,workbookDate1904:cfg.workbookDate1904,dateRange:dateValues.length?[dateValues[0],dateValues[dateValues.length-1]]:[],...result,totalAppointments:result.appointments.length}})}catch(error){postMessage({type:"ERROR",message:String(error?.message||error)})}};
